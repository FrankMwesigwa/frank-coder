export const generatePublicUrl = (fileName) => {
    return `http://localhost:7000/api/uploads/${fileName}`;
  };

// export const generatePublicUrl = (fileName) => {
// 	return `https://inthing.ug/api/uploads/${fileName}`;
// };
